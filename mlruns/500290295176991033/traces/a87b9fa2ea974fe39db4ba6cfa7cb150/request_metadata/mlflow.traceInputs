"{'query': \"SELECT \\n    (SELECT json_group_array(EVN_BULT_TIT_NM) \\n     FROM WLP_T_EVN_INF \\n     WHERE EVN_BULT_CN LIKE '%스타벅스%' \\n     AND EVN_SDT <= strftime('%Y%m%d', 'now') \\n     AND EVN_EDT >= strftime('%Y%m%d', 'now')) AS starbucks...